"""Analysis package for the AI Literacy & Trust Calibration study.

Modules
-------
config            : paths, column resolution, answer key, value mappings.
data_preparation  : load, parse, apply exclusions, score constructs.
reliability       : Cronbach's alpha, McDonald's omega, (optional) CFA.
descriptives      : means/SDs, correlation matrix, overconfidence test.
hypotheses        : the two pre-registered multiple regressions (H1, H2).
domain_analysis   : exploratory domain-specific trust analysis.
reporting         : formatting helpers and figure generation.

Run the whole pipeline with ``python run_analysis.py`` from the project root.
"""
